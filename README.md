# Training Web

FastAPI + static frontend dashboard for Training Dashboard.

## Repo boundary
- This repo owns the web API, UI, static assets, and NAS web deployment.
- Database schema and ETL builders live in the separate `training-etl` repo.
- Do not change schema from this repo unless explicitly coordinated.